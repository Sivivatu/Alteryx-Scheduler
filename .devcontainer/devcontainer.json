{
    "name": "Ayx-Scheduler",
    // "build": { "dockerfile": "Dockerfile" },
    "dockerComposeFile": "docker-compose.yml",
    "service": "app",
    "workspaceFolder": "/workspaces/Alteryx-Scheduler",
    // "runArgs": [ "--network", "host"],
    "postStartCommand": "pipenv install && cd frontend && npm install", // && pipenv shell", //pipenv install --system && sudo chown -R vscode:vscode /workspaces/Alteryx-Scheduler && 
    "customizations": {
      "vscode": {
        "extensions": [
            "patbenatar.advanced-new-file",
            "ms-python.python",
            "GitHub.copilot-chat",
            "GitHub.copilot",
            "ms-azuretools.vscode-docker",
            "bradlc.vscode-tailwindcss",
            "alexcvzz.vscode-sqlite",
            "charliermarsh.ruff",
            "ckolkman.vscode-postgres",
            "esbenp.prettier-vscode"
        ]
      }
    },
    "mounts": [
      "source=D:\\Alteryx-Scheduler,target=/workspaces/Alteryx-Scheduler,type=bind"
    ]
    // "remoteUser": "root"
  }
